import Login from '@/components/Login/Login';
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "CampusGuru University - Login",
};

const page = () => {
  
  return (
    <><Login /></>
  );
};

export default page;